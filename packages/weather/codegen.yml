overwrite: true
schema: "https://weather.devigraph.com/graphql"
documents: ./operations/**/*.graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
